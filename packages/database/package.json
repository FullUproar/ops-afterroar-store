{
  "name": "@afterroar/database",
  "version": "0.1.0",
  "private": true,
  "description": "Afterroar shared Prisma schema — canonical User, UserConsent, Passport, PointsLedger, pos_* tables. Single source of truth for the Afterroar side of the shared Neon database.",
  "scripts": {
    "generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  }
}
